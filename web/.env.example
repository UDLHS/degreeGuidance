# Auth.js (next-auth v5). Generate AUTH_SECRET with: openssl rand -hex 32
AUTH_SECRET=
AUTH_TRUST_HOST=true
# Base URL of the FastAPI backend (used for SERVER-SIDE calls only)
API_BASE_URL=http://127.0.0.1:8077
